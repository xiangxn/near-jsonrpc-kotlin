Enhanced codegen v3 with completed Main.kt (handles allOf, discriminator, BigInteger mapping and property-level serializer annotations).

Usage:
1. Generate sources:
   ./gradlew :codegen:run
2. Build:
   ./gradlew build
